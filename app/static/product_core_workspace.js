(() => {
  "use strict";
  const api = "/api/product-core/v1";
  const state = { personId: "", candidates: [], medications: [], timeline: [], brief: null, loadVersion: 0 };
  const byId = (id) => document.getElementById(id);
  const status = (message, kind = "") => { const target = byId("workspace-status"); target.textContent = message; target.className = kind; };
  const make = (tag, value = "", className = "") => { const node = document.createElement(tag); node.textContent = value; node.className = className; return node; };
  const clear = (node) => node.replaceChildren();
  const safeError = (response, body) => response.status === 422 ? "Check the entered values and try again." : response.status === 409 ? "This record changed. Refresh and try again." : response.status === 404 ? "That record is not available." : body?.error?.code === "product_core_storage_unavailable" ? "Local storage is temporarily unavailable." : "The request could not be completed. Try again.";
  async function request(path, options = {}) { const response = await fetch(api + path, { credentials: "same-origin", headers: { "Content-Type": "application/json" }, ...options }); let body; try { body = await response.json(); } catch (_) {} if (!response.ok) throw Error(safeError(response, body)); return body; }
  function enableWorkspace(enabled) { byId("workspace-content").setAttribute("aria-disabled", String(!enabled)); byId("workspace-content").querySelectorAll("input, textarea, select, button").forEach((item) => { item.disabled = !enabled; }); byId("include-all").checked = true; byId("medication-selection").disabled = true; }
  async function loadWorkspace() { const personId = byId("person-id").value; if (!personId.trim() || personId.length > 128) return status("Enter a valid person identifier.", "error"); const version = ++state.loadVersion; status("Loading workspace…"); try { const [candidates, medications, timeline] = await Promise.all([request(`/people/${encodeURIComponent(personId)}/candidates`), request(`/people/${encodeURIComponent(personId)}/medications`), request(`/people/${encodeURIComponent(personId)}/timeline`)]); if (version !== state.loadVersion) return; Object.assign(state, { personId, candidates: candidates.candidates, medications: medications.medications, timeline: timeline.events }); render(); enableWorkspace(true); status("Workspace loaded.", "success"); } catch (error) { status(error.message, "error"); } }
  function candidateCard(candidate, actions) { const card = make("article", "", "record"); card.append(make("strong", candidate.display_name), make("p", `Status: ${candidate.status} · Source: ${candidate.source_id} · Created: ${candidate.created_at}`, "meta")); if (candidate.schedule_text) card.append(make("p", candidate.schedule_text)); if (candidate.note) card.append(make("p", candidate.note)); if (candidate.predecessor_candidate_id) card.append(make("p", `Corrects: ${candidate.predecessor_candidate_id}`, "meta")); if (actions) { const confirmButton = make("button", "Confirm"); const correctButton = make("button", "Correct"); const rejectButton = make("button", "Reject"); confirmButton.type = correctButton.type = rejectButton.type = "button"; confirmButton.addEventListener("click", () => transition(candidate, "confirm", confirmButton)); correctButton.addEventListener("click", () => openCorrection(candidate, correctButton)); rejectButton.addEventListener("click", () => transition(candidate, "reject", rejectButton)); card.append(confirmButton, correctButton, rejectButton); } return card; }
  function render() { const inbox = byId("review-inbox"), history = byId("candidate-history"), medications = byId("canonical-medications"), timeline = byId("timeline"); [inbox, history, medications, timeline].forEach(clear); const pending = state.candidates.filter((item) => item.status === "pending"); if (!pending.length) inbox.append(make("p", "No medication entries are waiting for review.", "meta")); pending.forEach((item) => inbox.append(candidateCard(item, true))); const filter = byId("history-filter").value; (filter === "all" ? state.candidates : state.candidates.filter((item) => item.status === filter)).forEach((item) => history.append(candidateCard(item, false))); if (!history.childElementCount) history.append(make("p", "No candidates match this view.", "meta")); if (!state.medications.length) medications.append(make("p", "No medication records have been confirmed.", "meta")); state.medications.forEach((item) => medications.append(make("article", `${item.display_name} — Confirmed: ${item.confirmed_at} · Source: ${item.source_id} · Record: ${item.id}`, "record"))); if (!state.timeline.length) timeline.append(make("p", "No Product Core timeline events are available.", "meta")); state.timeline.forEach((item) => timeline.append(make("article", `${item.title} — ${item.event_at} · ${item.event_type} · Source: ${item.source_id} · Record: ${item.canonical_record_id}`, "record"))); const options = byId("brief-medication-options"); clear(options); state.medications.filter((item) => item.is_active).forEach((item) => { const label = document.createElement("label"); const input = document.createElement("input"); input.type = "checkbox"; input.name = "record"; input.value = item.id; label.append(input, document.createTextNode(` ${item.display_name}`)); options.append(label); }); }
  async function transition(candidate, action, button) { if (action === "reject" && !window.confirm("Reject this candidate?")) return; button.disabled = true; try { await request(`/candidates/${encodeURIComponent(candidate.id)}/${action}`, { method: "POST", body: "{}" }); await loadWorkspace(); status(`Candidate ${action}ed.`, "success"); } catch (error) { status(error.message, "error"); } finally { button.disabled = false; } }
  function openCorrection(candidate, trigger) { const form = document.createElement("form"); form.className = "record correction-form"; const name = document.createElement("input"), schedule = document.createElement("input"), note = document.createElement("textarea"); name.value = candidate.display_name; schedule.value = candidate.schedule_text || ""; note.value = candidate.note || ""; name.maxLength = 200; schedule.maxLength = 500; note.maxLength = 2000; const error = make("p", "", "error"); error.setAttribute("role", "alert"); const save = make("button", "Save correction"), cancel = make("button", "Cancel"); save.type = "submit"; cancel.type = "button"; const labelled = (label, control) => { const element = document.createElement("label"); element.textContent = label; element.append(control); return element; }; form.append(make("h3", "Correct medication entry"), labelled("Medication display name", name), labelled("Schedule text", schedule), labelled("Note", note), error, save, cancel); const close = () => { form.remove(); trigger.focus(); }; cancel.addEventListener("click", close); form.addEventListener("submit", async (event) => { event.preventDefault(); save.disabled = true; error.textContent = ""; try { await request(`/candidates/${encodeURIComponent(candidate.id)}/correct`, { method: "POST", body: JSON.stringify({ display_name: name.value, schedule_text: schedule.value || null, note: note.value || null }) }); await loadWorkspace(); close(); status("Correction is waiting for review.", "success"); } catch (failure) { error.textContent = failure.message; } finally { save.disabled = false; } }); trigger.closest("article").append(form); name.focus(); }
  byId("load-workspace").addEventListener("click", loadWorkspace); byId("clear-workspace").addEventListener("click", () => { Object.assign(state, { personId: "", candidates: [], medications: [], timeline: [], brief: null }); byId("person-id").value = ""; render(); enableWorkspace(false); status("Workspace cleared."); }); byId("history-filter").addEventListener("change", render); byId("include-all").addEventListener("change", () => { byId("medication-selection").disabled = byId("include-all").checked; }); byId("medication-form").addEventListener("submit", async (event) => { event.preventDefault(); const submit = event.submitter; submit.disabled = true; const display_name = byId("medication-name").value, schedule_text = byId("medication-schedule").value || null, note = byId("medication-note").value || null; try { const source = await request("/sources/manual-medication", { method: "POST", body: JSON.stringify({ person_id: state.personId, medication: { display_name, schedule_text, note } }) }); await request("/candidates/medications", { method: "POST", body: JSON.stringify({ person_id: state.personId, source_id: source.source.source_id, display_name, schedule_text, note }) }); event.target.reset(); await loadWorkspace(); status("Medication entry is waiting for review.", "success"); } catch (error) { status(error.message, "error"); } finally { submit.disabled = false; } }); enableWorkspace(false);
})();
