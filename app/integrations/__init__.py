"""Optional external integrations for OpenCare.

Modules under this package are never imported by OpenCare core; each
integration is opt-in and may depend on packages that are not installed in
the base environment.
"""
