"""Sentient Agent Framework integration (synthetic demo only).

The optional ``sentient`` extra installs ``sentient-agent-framework==0.3.0``.
Importing this package must never require the SDK: adapter code lives in
``adapter.py`` and ``demo.py``, which are imported only by callers that
explicitly opt in to the integration.
"""
