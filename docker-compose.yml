services:
  opencare:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPENCARE_ENV: local
      OPENCARE_DATA_DIR: data
      OPENCARE_REPORTS_DIR: reports
      OPENCARE_ALLOW_CLOUD_LLM: "false"
    volumes:
      - ./reports:/app/reports
